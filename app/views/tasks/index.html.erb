<h1>タスク一覧</h1>


  <table class="table table-striped table-bordered">
  <% @tasks.each do |task| %>
  <tr>  

    <td><%= link_to task.id, task %></td><td><%= task.status %></td><td> <%= task.content %></td>
 
  </tr>
   <% end %>
</table>
<%= link_to '新規タスクの投稿', new_task_path, class:"btn btn-primary" %>